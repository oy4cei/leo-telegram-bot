# 🚀 Розгортання Leo's Bot на Railway

Цей гід допоможе вам розгорнути бота на платформі Railway.

## Крок 1: Підготовка
1. Переконайтеся, що ваш код завантажено на **GitHub**.
2. Зареєструйтеся на [Railway.app](https://railway.app/) (можна через GitHub).

## Крок 2: Створення проекту
1. Натисніть **New Project** -> **Deploy from GitHub repo**.
2. Виберіть репозиторій з ботом.
3. Натисніть **Deploy Now**.

## Крок 3: Налаштування змінних (Environment Variables)
1. Перейдіть у вкладку **Variables**.
2. Додайте змінну:
   - `TELEGRAM_BOT_TOKEN`: ваш токен від BotFather.
   - `TZ`: `Europe/Kiev` (щоб час у звітах був правильним).

## Крок 4: Налаштування бази даних (Щоб дані не зникали!)
Оскільки Railway видаляє файли при кожному перезапуску, нам потрібно створити "Volume" (постійне сховище) для бази даних SQLite.

1. Відкрийте ваш сервіс у Railway.
2. Перейдіть у вкладку **Volumes**.
3. Натисніть **Create Volume**.
4. Вкажіть Mount Path: `/app/data` (або просто `/data`).
5. Перейдіть назад у вкладку **Variables** і додайте:
   - `DB_PATH`: `/app/data/leo_tracker.db` (шлях має відповідати Mount Path).

## Крок 5: Перезапуск
Після додавання змінних Railway автоматично перезапустить бота.
Тепер база даних буде зберігатися у Volume і не зникне при оновленнях!

## Корисні команди
- Щоб подивитися логи: вкладка **Logs**.
- Щоб перезапустити вручну: кнопка **Restart** у меню сервісу.
